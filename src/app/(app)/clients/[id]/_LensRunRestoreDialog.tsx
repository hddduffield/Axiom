"use client";

// Phase 15.2 — placeholder. Real implementation lands in the 15.2 commit
// alongside the show-archived toggle.

export function LensRunRestoreDialog(_props: {
  lensRunId: string;
  scenarioName: string;
  lensTypeLabel: string;
}) {
  return null;
}
